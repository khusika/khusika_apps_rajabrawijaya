<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pit.rajabrawijaya">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        >

        <activity
            android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ScanActivity" />

        <activity android:name=".ProfileActivity" />

        <activity
            android:name=".RajaActivity"
            android:theme="@style/AppTheme2" />

        <activity
            android:name=".PK2MUActivity"
            android:theme="@style/AppTheme2" />

        <activity
            android:name=".PBPKActivity"
            android:theme="@style/AppTheme2" />

        <activity
            android:name=".OHLKMActivity"
            android:theme="@style/AppTheme2" />

        <activity
            android:name=".ohlkm.EMActivity"
            android:theme="@style/AppTheme2" />
        <activity
            android:name=".ohlkm.DPMActivity"
            android:theme="@style/AppTheme2" />
        <activity
            android:name=".ohlkm.KongresActivity"
            android:theme="@style/AppTheme2" />

    </application>

</manifest>